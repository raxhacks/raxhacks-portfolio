/**
 * Using custom cursor make the terminal 
 * more authentic.
 */
export default function CustomCaret() {
    return (
        <div className="w-[0.6em] h-[1.1em] 
        bg-[#18B06A] animate-blink self-center">
        </div>
    );
}